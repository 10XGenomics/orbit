[package]
name = "orbit"
version = "0.1.0"
authors = ["mkirsche", "Lance Hepler <lance.hepler@10xgenomics.com>", "Patrick Marks <patrick@10xgenomics.com>"]
edition = "2018"
build = "build.rs"

[dependencies]
libc = "*"
rust-htslib = { version = "0.26", default-features = false }
failure = "*"

[build-dependencies]
cc = { version = "1.0", features = ["parallel"] }
