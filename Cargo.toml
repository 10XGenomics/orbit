[package]
name = "orbit"
version = "0.1.0"
authors = ["mkirsche", "Lance Hepler <lance.hepler@10xgenomics.com>", "Patrick Marks <patrick@10xgenomics.com>"]
edition = "2018"
build = "build.rs"

[dependencies]
libc = "*"
rust-htslib = { version = "0.26", default-features = false, features = ["serde"] }
failure = "*"

[build-dependencies]
cc = "^1.0"
fs-utils = "*"

